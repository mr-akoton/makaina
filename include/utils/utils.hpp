#ifndef UTILS_HPP
# define UTILS_HPP

# include <utils/types.hpp>
# include <string>

std::string	getFileContent(const char* file);
Vector3		hexToRGB(std::string color);

#endif /* UTILS_HPP ========================================================= */
